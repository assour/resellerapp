import { createMockMarketplaceAdapter } from './mockAdapterFactory';

// Production: use TikTok Shop Partner Center APIs after app approval and seller authorization.
export const tiktokShopAdapter = createMockMarketplaceAdapter('tiktok');
